# Before / after comparisons

Open the gallery:

```bash
npm run compare
```

→ http://localhost:5176

Screenshots live in `shots/`. Re-capture (requires `original` on :5175 and `exact` on :5173):

```bash
npm run original   # terminal 1
npm run exact      # terminal 2
npm run capture-compare
```
